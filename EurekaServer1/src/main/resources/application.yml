server:
  port: 8888
spring:
  application:
    name: eurekaServer
eureka:
  client:
    register-with-eureka: true #不再注册到其他注册中心
    fetch-registry: true #不从其他的注册中心拉取服务器信息
    service-url:
      defaultZone: http://localhost:9999/eureka
  instance:
    prefer-ip-address: true
